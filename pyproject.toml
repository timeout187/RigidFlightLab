[project]
name = "rigidflightlab"
version = "0.1.0"
description = "Academic 6-DOF spin-stabilized projectile simulator - published-benchmark reproduction and numerical-methods education only."
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
